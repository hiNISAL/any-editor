export default {
  // 行高列表
  LINE_HEIGHT: [1, 1.15, 1.5, 2, 3],
  // 字体相关自定配置
  FONT: {
    // 是否使用pt(字号)作为单位，默认px作为单位
    USE_PT: false,
    // 字体列表
    LIST: [9, 10, 12, 14, 16, 18, 20, 22, 14, 26, 28, 32, 36],
    // 字体族
    FAMILY: [],
  },
};
